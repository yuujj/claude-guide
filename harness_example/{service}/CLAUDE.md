# CLAUDE.md — {service}

> 레포 루트 `CLAUDE.md`(안전·인프라)와 함께 로드됩니다.
> 이 파일은 **{service} 의 코드·도메인 규칙**입니다.

{서비스_한줄_설명 — 예: 무엇을 받아 무엇을 어떻게 처리하는 서비스인지 1~2문장}

## 1. 절대 규칙

<!-- 💡 위반하면 안 되는 코드/도메인 불변식. 각 규칙은 "왜"가 있으면 ADR 번호로 인용. -->

- 언어/런타임: {언어_버전}. 패키지 관리: **{패키지매니저}**. 린트/포맷: **{린터}**.
- {핵심_규칙_1 — 예: 모든 X 요청은 반드시 Y를 동반한다. 누락 시 거부 (ADR-00N)}.
- {핵심_규칙_2 — 예: 외부 호출은 직접 하지 않고 항상 어댑터 경유}.
- {핵심_규칙_3 — 예: PII/민감정보는 로그에 평문 출력 금지, 마스킹 함수 경유}.

## 2. 프로젝트 구성

| 기능 | 진입점 | 핵심 모듈 | 설정/리소스 | 라우터/큐 |
|------|--------|----------|------------|--------|
| {기능1} | `{진입점}` | `{핵심모듈}` | {리소스} | {라우터} |
| {기능2} | `{진입점}` | `{핵심모듈}` | {리소스} | {라우터} |

공통 인프라: {공통_경로들 — 예: `db/`, `utils/`, `config/`}

## 3. 도메인 규칙 (추측 금지 — 상수표)

<!-- 💡 ⚠️ 가장 중요한 표. 에이전트가 절대 지어내면 안 되는 도메인 상수를 못박습니다.
     상태값·열거형·키 형식·임계값 등. 여기 없는 값을 코드에 새로 만들지 못하게 합니다. -->

| 규칙 | 내용 |
|------|------|
| {열거형1 — 예: 상태값} | {허용값 나열 — 예: A → B → C / D} |
| {열거형2} | {허용값} |
| {키_형식 — 예: 식별자 포맷} | {형식 — 예: `<x>.<y>`} |
| {임계값 — 예: 제한·윈도우} | {값} |

> ⚠️ **변경 금지 상수(공식·횟수 등)는 여기에 적지 말고, 설계 문서 한 곳(SSOT)에만 두고 인용하세요.** 예: 재시도 공식 → `docs/design/{feature}/reliability_design.md` 참조.

## 4. 코드 규약

- {규약_1 — 예: 인터페이스/베이스 클래스 상속 규칙}
- {규약_2 — 예: DB 모델 스타일, 마이그레이션 도구}
- {규약_3 — 예: 설정 로딩 방식}
- {규약_4 — 예: 로깅·trace_id 전파 규칙}

## 5. 금지사항

<!-- 💡 결정된 사항을 임의로 바꾸지 못하게. ADR 번호로 근거 인용. -->

- {금지_1 — 예: ○○ 공식을 임의 변경하지 않는다 (ADR-00N 결정 사항)}.
- `docs/archive/` 파일 삭제 금지. 이력 보존 목적.
- `docs/tasks/`에 보고서/분석을 넣지 않는다. 보고서는 `docs/reports/`.

## 6. 문서 체계

<!-- 💡 아래 @import는 Claude Code가 docs/INDEX.md를 매 세션 자동 로드하게 합니다.
     (산문 "먼저 읽을 것"보다 확실 — 에이전트가 INDEX를 빠뜨리지 않음)
     경로는 이 CLAUDE.md 기준 상대경로입니다. -->

@docs/INDEX.md

| 경로 | 역할 |
|------|------|
| `docs/INDEX.md` | 문서 인덱스 (진입점 — 위 @import로 자동 로드됨) |
| `docs/product/<feature>/` | 제품 정의 — PRD, 기능 정의서, ADR |
| `docs/design/<feature>/` | 기술 설계 — 아키텍처, 품질 속성 설계 |
| `docs/reports/` | 보고서/분석 — 부하/장애 분석, 벤치마크 |
| `docs/tasks/` | 태스크 — 순수 실행 계획만 |
| `docs/guide/` | 가이드 — 보고서 양식, 반복 작업 마스터 지도 |
| `docs/reference/` | 참조 — 실행 가이드, 변경 이력 (매 세션 참조) |
| `docs/archive/` | 보관 — 완료/대체된 문서 (삭제 금지) |

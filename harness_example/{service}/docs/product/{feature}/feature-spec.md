# 기능 정의서 — {기능명}

> 기능의 **무엇을/어떻게 동작하는가**를 정의. 입출력·분기·엣지 케이스까지.
> 결정의 *이유*는 ADR, 구현 구조는 design/architecture.md 참조.
> 최종 갱신: {YYYY-MM-DD}

---

## 1. 처리 파이프라인 / 플로우

<!-- 💡 핵심 흐름을 단계로. ASCII 다이어그램 권장. -->

```
{단계1} → {단계2} → {단계3} → ...
```

| 단계 | 처리 | 실패 시 |
|------|------|---------|
| {1} | {처리} | {실패 동작 — 예: 422 반환} |
| {2} | {처리} | {실패 동작} |
| {3} | {처리} | {실패 동작} |

---

## 2. API / 인터페이스 스펙

<!-- 💡 외부에 노출되는 계약. 요청/응답/오류를 구체적으로. API가 없으면 함수/CLI 시그니처로. -->

### `{메서드 경로 — 예: POST /v1/...}`

요청:
```jsonc
{
  "{필드1}": "{타입/예시}",
  "{필드2}": "{타입/예시}"
}
```

응답 ({성공코드}):
```jsonc
{ "{응답필드}": "{예시}" }
```

오류 응답 (규약: 본문에 `error` 키):
```jsonc
{ "error": "{에러코드}", "detail": "{설명}" }
```

| 코드 | 상황 |
|------|------|
| {2xx} | {성공 상황} |
| {4xx} | {검증 실패 상황} |
| {5xx} | {시스템 실패 상황} |

---

## 3. 상태 머신

<!-- 💡 상태가 있는 기능이면 전이도. 없으면 이 섹션 삭제. -->

```
{상태A} ──({이벤트})──> {상태B} ──({이벤트})──> {상태C}
                          └──({실패})──> {상태D}
```

- {상태_정의_보충 — 예: failed=일시 실패, dead=최종 실패}

---

## 4. 엣지 케이스

<!-- 💡 "이럴 땐 어떻게?"를 미리 고정. 에이전트가 임의 판단하지 않게. -->

| 상황 | 동작 |
|------|------|
| {엣지1} | {동작} |
| {엣지2} | {동작} |
| {엣지3} | {동작} |

---

## 5. 변형/채널별 차이

<!-- 💡 같은 기능이 대상별로 다르게 동작하면 표로. 없으면 삭제. -->

| 항목 | {변형A} | {변형B} | {변형C} |
|------|-------|-----|------|
| {속성1} | {값} | {값} | {값} |
| {속성2} | {값} | {값} | {값} |

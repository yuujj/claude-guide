# ADR — {기능명}

> Architecture Decision Record. **왜 이렇게 결정했는가**를 누적 기록한다.
> 한 번 쓴 ADR은 지우지 않는다. 번복 시 "ADR-N을 ADR-M이 대체함"으로 새 항목 추가.
>
> 형식: 상태(Accepted/Superseded/Proposed) · 맥락 · 결정 · 결과(트레이드오프).

<!-- 💡 아래 ADR-001은 작성 형식을 보여주는 예시 골격입니다.
     실제 결정으로 교체하고, 결정이 늘면 같은 형식으로 ADR-002, 003... 추가하세요. -->

---

## ADR-001 — {결정_제목 — 예: A 대신 B를 채택}

- **상태**: {Accepted / Superseded / Proposed} ({YYYY-MM-DD})
- **맥락**: {왜 이 결정이 필요했나. 당시 문제 상황.}
- **결정**: {무엇을 하기로 했나. 구체적으로.}
- **대안 검토**: {고려했으나 기각한 대안 + 기각 이유. 없으면 줄 삭제.}
- **결과**:
  - (+) {장점/얻은 것}
  - (−) {단점/감수한 것}
  - {대체 관계가 있으면: 기존 X 문서는 `archive/...`로 보관.}

<!-- 💡 ⚠️ 이 결정이 "변경 금지 상수"(공식·횟수 등)를 만든다면:
     - 구체 수치는 여기 서술하되, 운영용 표는 design/{feature}/reliability_design.md 한 곳(SSOT)에만 둡니다.
     - CLAUDE.md 금지사항에서 "이 값 변경 금지 (ADR-001)"로 인용합니다. -->

---

## ADR-002 — {결정_제목}

- **상태**: {상태} ({YYYY-MM-DD})
- **맥락**: {맥락}
- **결정**: {결정}
- **결과**:
  - (+) {장점}
  - (−) {단점}

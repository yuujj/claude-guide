# 실행 가이드

> **매 세션 참조.** 환경 세팅과 로컬 실행/테스트 방법. 분량은 짧게 유지한다.
> ⚠️ 모든 명령은 **루트 `CLAUDE.md` §0의 셸 문법**으로 작성한다.

---

## 1. 환경 세팅

```{셸}
{의존성_설치 — 예: uv sync / npm install}
{환경변수_준비 — 예: cp config/.env.example .env}
```

신규 패키지 추가: `{패키지_추가_명령}` <!-- 💡 예: uv add <pkg> (pip 직접 사용 금지) -->

---

## 2. 로컬 인프라

<!-- 💡 DB/브로커 등 의존 인프라 기동. 없으면 삭제. -->

```{셸}
{인프라_기동 — 예: docker compose up -d redis postgres}
{마이그레이션 — 예: alembic upgrade head}
```

---

## 3. 앱 / 워커 실행

```{셸}
# {프로세스1 — 예: API}
{실행_명령}

# {프로세스2 — 예: 워커}
{실행_명령}
```

---

## 4. 동작 테스트

<!-- 💡 "잘 도는지" 확인하는 최소 호출 예시. -->

```{셸}
{호출_예시 — 예: curl -X POST localhost:8000/...}
# → {기대_응답}

{상태_조회 예시}
```

> {환경_주의 — 예: dev는 샌드박스 키 → 실제 발송 안 됨. 대시보드 sandbox 로그로 확인 (CLAUDE.md §7)}.

---

## 5. 테스트 / 린트

```{셸}
{테스트_명령 — 예: pytest -q}
{린트_명령 — 예: ruff check . && ruff format --check .}
```

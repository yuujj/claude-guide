# 아키텍처 — {기능명}

> 기술 설계. **어떻게 구현하는가**. 결정의 이유는 ADR, 동작 정의는 feature-spec 참조.
> 최종 갱신: {YYYY-MM-DD}

---

## 1. 디렉토리 구조

<!-- 💡 실제 코드 트리. 각 디렉토리/파일에 한 줄 역할 주석. -->

```
{service}/
├── {디렉토리1}/          # {역할}
│   └── {파일}            # {역할}
├── {디렉토리2}/          # {역할}
├── {공통}/               # {역할 — 예: utils, config}
└── {리소스}/             # {역할}
```

---

## 2. 컴포넌트 다이어그램

<!-- 💡 컴포넌트 간 데이터 흐름. ASCII로 충분. -->

```
{컴포넌트A} ──{동작}──▶ {컴포넌트B} ──{동작}──▶ {컴포넌트C}
```

---

## 3. 데이터 모델 (ERD 요약)

<!-- 💡 주요 엔티티와 핵심 필드. PK/FK/unique 제약 표시. 없으면 삭제. -->

```
{엔티티1}
  {id} (PK)            {필드}            {상태 필드}
  {필드}               {필드}

{엔티티2}
  {key} (PK)           {FK}              {created_at}
```

- {모델_주의사항 — 예: 원본 PII는 저장하지 않고 마스킹 결과만 저장 (CLAUDE.md §1)}.
- {제약_근거 — 예: unique 제약 → ADR-00N 근거}.

---

## 4. 핵심 시퀀스 (정상 경로)

```
{호출자} → {진입점}:
  1. {단계}
  2. {단계}
  3. {단계}

{비동기/워커 측 — 있으면}:
  4. {단계}
  5. {단계}
```

자세한 실패 분기·재시도는 `reliability_design.md` 참조.

---

## 5. 기술 스택

| 영역 | 선택 | 비고 |
|------|------|------|
| {영역1} | {선택} | {근거 — 예: ADR-00N} |
| {영역2} | {선택} | {근거} |
| {영역3} | {선택} | {근거} |

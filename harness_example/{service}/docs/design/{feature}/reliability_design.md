# {품질속성} 설계 — {예: 멱등성·재시도·DLQ 등}

> 관련 ADR을 구현 레벨로 푼 상세 설계. 실패 모드와 처리를 표로 고정한다.
> 최종 갱신: {YYYY-MM-DD}

> ⚠️ **SSOT(단일 진실 공급원) 문서**
> 이 프로젝트의 **변경 금지 상수·공식**(재시도 횟수, 백오프 지연, 임계값 등)은
> **이 문서의 표에만** 정의합니다. ADR·CLAUDE.md·feature-spec 등 다른 문서는
> 값을 **복제하지 말고** "→ 이 문서 §N 참조"로 **인용만** 하세요.
> (같은 숫자를 두 곳에 적으면 반드시 어긋납니다.)

---

## 1. {품질속성_1 — 예: 멱등성}

<!-- 💡 의사코드 또는 규칙으로 동작을 고정. -->

```
{의사코드 또는 규칙}
```

- {핵심_보장 — 예: 동시 2건이 같은 키로 와도 한 건만 생성}.
- {부수_규칙 — 예: 24h 후 정리 배치}.

---

## 2. 재시도 / 백오프 (SSOT 표)

<!-- 💡 ⚠️ 이 표가 재시도 정책의 유일한 정의입니다. 다른 문서는 이 표를 인용만 하세요.
     아래는 형식 예시이며, 본 프로젝트 값으로 교체하세요. 횟수 정의("N회"가 총 시도인지
     재시도 횟수인지)를 표 안에서 명확히 하여 해석 충돌을 막으세요. -->

| 시도(n) | 지연 | 누적 |
|---------|------|------|
| {1} | {즉시} | {0} |
| {2} | {지연} | {누적} |
| {...} | {...} | {...} |
| ({최대 N} 실패) | — | → {최종상태} + {DLQ 등} |

- 지연 공식 = `{공식 — 예: 2^(n-2)분}`. **"N회"의 정의**: {여기서 N회 = 총 시도 횟수 / 재시도 횟수 중 무엇인지 명시}.
- **이 표는 임의 변경 금지** (CLAUDE.md / ADR-00N).

---

## 3. 실패 모드 표 (핵심)

<!-- 💡 "어떤 실패에 재시도하고, 어떤 실패는 즉시 포기하는가"를 못박습니다. -->

| 실패 원인 | 신호 | 재시도? | 최종 상태 |
|-----------|------|--------|-----------|
| {일시 장애} | {5xx/timeout} | O ({최대 N회}) | {회복 시 X, 소진 시 Y} |
| {영구 실패} | {4xx} | X | {즉시 Y} |
| {인증 실패} | {401/403} | X | {Y + 운영 알람} |
| {레이트 리밋} | {429} | O | {백오프 후 회복} |

---

## 4. DLQ / 최종 실패 처리

<!-- 💡 최종 실패를 어디에 쌓고, 자동 재처리 여부. -->

- {최종실패_적재 — 예: dead 전이 시 스냅샷을 DLQ에 적재}.
- 용도: {수동 조사·재처리}. {자동 재처리 안 함 — 루프 방지}.
- 모니터링: {임계 알람 — 예: DLQ 적재율 > 0.1% (PRD 성공 기준)}.

---

## 5. {품질속성_2 — 예: 시간 윈도우 / 레이트 제한}

<!-- 💡 해당 없으면 삭제. -->

```
{규칙 또는 의사코드}
```

- {보충 규칙}.

# CLAUDE.md — {프로젝트명}

> 글로벌 `~/.claude/CLAUDE.md` 와 함께 로드됩니다. 충돌 시 더 제한적인 쪽이 우선합니다.
> 이 파일은 **레포 전체의 안전·인프라 가드레일**입니다. 코드/도메인 규칙은
> 각 서비스의 `<service>/CLAUDE.md`(예: `{service}/CLAUDE.md`)에 둡니다.

---

## 0. 실행 환경 (명령어 작성 필수 참고)

<!-- 💡 명령어 예시 문법이 여기서 갈립니다. 반드시 본인 개발 환경으로 교체하세요. -->

- **OS**: {OS — 예: Windows 11 / macOS 14 / Ubuntu 22.04}
- **Shell**: {셸 — 예: PowerShell / bash / zsh}
- 컨테이너 런타임: {컨테이너_런타임 — 예: Docker 24+, 없으면 줄 삭제}
- 모든 명령어 예시는 위 셸 문법으로 제공할 것. <!-- 💡 예: PowerShell이면 $null/$env:VAR, bash면 /dev/null/$VAR -->

---

## 1. 프로젝트 Identity

- **프로젝트명**: {프로젝트명} ({프로젝트_한줄_설명})
- **목적**: {프로젝트_목적 — 무엇을 해결하는가}
- **저장소**: {저장소_URL}
- **개발 브랜치**: `{개발_브랜치 — 예: develop}` / **메인 브랜치**: `{메인_브랜치 — 예: main}`

---

## 2. 절대 금지 사항 (가드레일)

<!-- 💡 에이전트가 절대 직접 실행하면 안 되는 명령을 구체적으로. 추상적 표현 X, 명령어 단위로.
     본 프로젝트의 위험 작업(배포·DB 상태변경·시크릿 등)에 맞게 교체하세요. -->

```
{금지_1 — 예: git push / merge / rebase / commit / branch 생성·삭제·체크아웃 (사용자가 직접 수행)}
{금지_2 — 예: 보호 브랜치(main/develop) 및 인프라 매니페스트 직접 수정}
{금지_3 — 예: 프로덕션 환경에서의 상태 변경 명령}
{금지_4 — 예: 자격증명/시크릿을 코드·로그·테스트에 하드코딩}
```

- 에이전트는 **diff 제안, 커밋 메시지 초안, 전략 제시**까지만 수행.
- 실제 저장·커밋·푸시·배포는 **사용자가 직접 수행**.

---

## 3. 환경 / 배포 구조 (조회 전용)

<!-- 💡 배포 대상이 없는 프로젝트면 이 섹션 전체를 삭제해도 됩니다. -->

| 환경 | 접속/컨텍스트 | 네임스페이스/위치 | 레지스트리/호스트 |
|---|---|---|---|
| {Dev} | `{dev_context}` | `{dev_ns}` | `{dev_registry}` |
| {Prod} | `{prod_context}` | `{prod_ns}` | `{prod_registry}` |

**Pre-flight 체크** (변경 전 필수):

```
{pre_flight_명령 — 예: 현재 컨텍스트/브랜치/환경을 출력하는 명령들}
```
출력에 `{위험_환경_키워드 — 예: prod}` 가 보이면 **즉시 중단** 후 사용자 확인.

---

## 4. 서비스 구성 요약

| 서비스 | 경로 | 기술 스택 | 포트 | 비고 |
|---|---|---|---|---|
| {서비스1} | `{service}/` | {기술스택} | {포트} | {비고 — 예: 본 하네스의 주 대상} |
| {서비스2} | `{경로}/` | {기술스택} | {포트} | {비고} |
| {인프라} | `{경로}/` | {기술스택} | — | 조회만 |

---

## 5. CI/CD 구조 요약

<!-- 💡 CI/CD가 없으면 삭제. 있으면 트리거 방식과 배포 흐름만 간단히. -->

```
{CI_시스템 — 예: GitHub Actions (.github/workflows/...)}
  └─ {트리거 — 예: workflow_dispatch (수동만)}
       ├─ {빌드 단계}
       └─ {배포 단계}
```

---

## 6. 외부 서비스 / 환경변수 참조

| 서비스 | 환경변수 키 | 관리 위치 |
|---|---|---|
| {외부서비스1} | `{ENV_KEY_1}` | {관리위치 — 예: k8s Secret / .env / Secret Manager} |
| {외부서비스2} | `{ENV_KEY_2}` | {관리위치} |

---

## 7. 알려진 이슈 / 주의사항

<!-- 💡 "에이전트가 모르면 헛다리 짚는" 함정을 적습니다. 예: 환경별 차이, 분리된 워커, 필수 키 등. -->

1. {주의사항_1}
2. {주의사항_2}
3. {주의사항_3}

---

## 8. 파일 수정 워크플로우

1. 파일을 반드시 직접 읽는다. 2. 변경을 2~3줄로 요약. 3. 영향 파일 목록.
4. diff 제시. 5. 검증 커맨드 제시. 6. 인프라 변경이면 롤백 경로 포함.
7. 저장·커밋·배포는 **사용자가 직접 수행**.

---

## 9. Definition of Done

<!-- 💡 §2 금지사항과 1:1로 대응시키면 작업 종료 시 자기검증이 됩니다. -->

- [ ] 위 절대 금지 사항 위반 없음
- [ ] git write / 배포 / 상태 변경 명령 미수행 (조회만)
- [ ] {위험_환경} 이 아님을 pre-flight로 확인
- [ ] 자격증명/시크릿을 코드·로그에 포함하지 않음
- [ ] diff 제안 전 해당 파일을 실제로 읽었음
- [ ] 문서 추가/이동 시 `{service}/docs/INDEX.md` 갱신 포함
